export enum ELocalStorageKey {
  AccessToken = 'accessToken',
}

export enum EMedia {
  Image = 'image',
  Video = 'video',
}

export enum EViewMode {
  Block = 'block',
  Flex = 'flex',
  Grid = 'grid',
  None = 'none',
}

export enum EFormType {
  View = 'view',
  Update = 'update',
  Create = 'create',
  Delete = 'delete',
  Report = 'report',
}
