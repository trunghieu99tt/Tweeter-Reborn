export enum EGender {
  MALE = 0,
  FEMALE = 1,
  UNKNOWN = 2,
}

export enum EUserListType {
  Liked = 'liked',
  Saved = 'saved',
  Retweeted = 'retweeted',
  Following = 'following',
  Followed = 'followed',
}
